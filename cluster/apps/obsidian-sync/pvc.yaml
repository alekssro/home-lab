---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: obsidian-sync-pvc
  namespace: syncthing
  labels:
    app: obsidian-sync
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 5Gi
