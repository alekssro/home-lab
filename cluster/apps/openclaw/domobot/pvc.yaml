---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openclaw-domobot-home-pvc
  namespace: openclaw
  labels:
    app: openclaw-domobot
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
