apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homarr-appdata-nfs
  namespace: homarr
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 1Gi
