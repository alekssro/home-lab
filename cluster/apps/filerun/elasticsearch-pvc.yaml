apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filerun-elasticsearch-pvc
  namespace: filerun
  labels:
    service: filerun-elasticsearch
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
