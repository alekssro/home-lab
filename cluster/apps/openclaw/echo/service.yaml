---
apiVersion: v1
kind: Service
metadata:
  name: openclaw-echo
  namespace: openclaw
  labels:
    app: openclaw-echo
spec:
  type: ClusterIP
  selector:
    app: openclaw-echo
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
