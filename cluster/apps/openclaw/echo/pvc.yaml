---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openclaw-echo-home-pvc
  namespace: openclaw
  labels:
    app: openclaw-echo
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
