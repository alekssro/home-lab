apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-home-2
  namespace: networking
  labels:
    service: adguard-home-2
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200Mi
