---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openclaw-chronos-home-pvc
  namespace: openclaw
  labels:
    app: openclaw-chronos
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
