apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filerun-db-pvc
  namespace: filerun
  labels:
    service: filerun-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
